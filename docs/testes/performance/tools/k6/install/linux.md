# 🐧 Linux (Debian/Ubuntu)

No Debian ou Ubuntu, você pode instalar o K6 diretamente via apt:

```bash
sudo apt update
sudo apt install k6
```

---

## ✅ Verificação da instalação

Após a instalação, confirme se o K6 foi instalado corretamente:

```bash
k6 version
```
